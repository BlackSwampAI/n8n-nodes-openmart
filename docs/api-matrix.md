# Openmart API matrix

Sources were reviewed through September 7, 2026. Documentation and automated tests are not live API evidence. The user separately reported successful live Balance and Business Search runs; the agent did not observe authenticated responses or make a paid request.

| Resource / operation         | Value                                 | Method and path                                 | Controls / minimum state                                                                                  | Request and output                                                                                       | Side effects                                              | Status and test tier                                                         | Evidence and unresolved questions                                                                                                                                                                                                                |
| ---------------------------- | ------------------------------------- | ----------------------------------------------- | --------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Business / Search            | Find prospect businesses              | `POST /api/v1/search`                           | Required query; limit 1–100; optional one-location array and initial top-level filters                    | Sends `estimate_total:false` without cursor; validates documented top-level result array                 | Potentially credit-consuming; never automatically retried | Implemented; metadata/hook/fixture/package tests; user-reported live success | [Search](https://app.openmart.com/api-docs/search-businesses). User report was not agent-observed. General max is documented as 1000, but preview keys are capped at 100, so UI uses conservative 100. Country examples vary `US`/`USA`.         |
| Business / Get               | Retrieve records by Openmart ID       | `GET /api/v1/business_records/list/openmart_id` | Planned: 1–100 IDs                                                                                        | JSON array body; response keyed by found ID; missing IDs omitted                                         | Read request                                              | Planned, transport unresolved; docs/loopback only; not advertised            | [Business lookup](https://app.openmart.com/api-docs/get-business-by-openmart-id). n8n 2.37.10's recommended authenticated helper strips GET bodies; deprecated legacy transport preserved one in loopback. No transport or fallback is selected. |
| People Search / Create       | Start decision-maker discovery        | `POST /api/v1/task/batch/find_people`           | Domain; title default `Owner or decision maker`; max 1–8; EMAIL/PHONE; optional company/location/tracking | One-element JSON-array creation; returns validated provider envelope plus normalized `submitted` context | Paid work; 90s timeout; no internal retry or polling      | Implemented; metadata/hook/package tests; live paid call pending             | [Decision makers](https://app.openmart.com/api-docs/find-decision-makers). Resolve entitlements, billing, task states, tracking ID, and missing results live.                                                                                    |
| Company Email / Create       | Find a generic company mailbox        | `POST /api/v1/task/batch/lookup_business_email` | Required normalized domain and company name; optional city/state/country/tracking ID                      | One-element JSON-array creation; returns validated provider envelope plus normalized `submitted` context | Paid work; 90s timeout; no internal retry or polling      | Implemented; metadata/hook/package tests; live paid call pending             | [Find Company Emails](https://app.openmart.com/api-docs/find-company-emails). Nonblank `submit_for` is preserved due conflicting labels. Entitlement, billing, states, and results remain live gaps. `notify_url` is not exposed.                |
| Batch / Get Status           | Inspect existing batch progress       | `GET /api/v1/task/batch/{batch_id}/status`      | Required trimmed batch ID                                                                                 | Validates readiness/counts; preserves unknown fields and appends requested `batch_id` for correlation    | Declarative safe read; no internal retry                  | Implemented; tests/package and n8n 2.37.10 metadata; live API pending        | [Batch status](https://app.openmart.com/api-docs/check-batch-status). Actual state set and partial-failure behavior unresolved.                                                                                                                  |
| Batch / Get Task IDs         | List tasks in a batch                 | `GET /api/v1/task/batch/{batch_id}/task_ids`    | Required trimmed batch ID; optional free-text status, documented example `COMPLETED`                      | Atomically validates string array; emits `{task_id,batch_id}` per task; empty emits none                 | Declarative safe read; no internal retry                  | Implemented; tests/package and n8n 2.37.10 metadata; live API pending        | [Task IDs](https://app.openmart.com/api-docs/get-task-ids-in-a-batch). Other states and missing-task behavior unresolved.                                                                                                                        |
| Task / Get                   | Retrieve task and results             | `GET /api/v1/task/{task_id}`                    | Required trimmed task ID                                                                                  | Validates task ID/status; permits optional/null tracking ID and varying/missing data; preserves envelope | Declarative safe read; no internal retry                  | Implemented; tests/package and n8n 2.37.10 metadata; live API pending        | [Task results](https://app.openmart.com/api-docs/get-task-results). State, tracking, empty, and missing-result behavior require live validation.                                                                                                 |
| Account / Get Credit Balance | Inspect credits and test connectivity | `GET /api/v2/credit-balance`                    | Openmart API credential; no node controls                                                                 | Validate RFC3339 period strings and integer `balance`; preserve provider fields                          | Harmless read                                             | Implemented; unit/hook/package tests; user-reported live success             | [Credit balance](https://app.openmart.com/api-docs/get-credit-balance). User report was not agent-observed; this implementation run made no live call. Unknown keys are documented as HTTP 401.                                                  |

The production origin is fixed at `https://api.openmart.ai`, with recommended `Authorization: Bearer <api_key>` authentication. Search exposes only `has_website`, `has_valid_website`, `has_contact_info`, and positive-integer `min_locations`; blank location fields are omitted. Tags, other filters, cursors, pagination, and later operations remain deliberately absent.

All advertised operations use n8n declarative routing and make one request per input item without hidden retries. Users may opt into standard n8n retry settings for safe reads. Search and paid creation are not retried internally. Enabling n8n Retry On Fail or manually rerunning paid creation can create duplicate charged work.

For 0.1.0, the shared Batch Status → Task IDs → Task Get retrieval chain and a demonstrated Search → email/contact discovery → downstream data handoff are release-critical, with fixtures and workflow evidence for both branches where appropriate. The node provides data but does not send outreach. Company Emails means generic shared inboxes; Find Decision Makers means named individual contacts and emails.

Future-release/backburner operations, not implemented or advertised, are: Detect Tech Stack; Search Business IDs Fast; Get by Google Place ID; Enrich Company; Enrich Known People; Search Companies; Create Deny Rules; Check Deny Rules; Delete Deny Rules.
